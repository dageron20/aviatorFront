import { extendTheme } from '@chakra-ui/react'
export const overrides = extendTheme({
    components: {
    },
    breakpoints: {
        sm: '320px',
        md: '768px',
        lg: '1280px',
        xl: '1920px',
    },
})